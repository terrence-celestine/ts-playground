// Runs every generics demo in sequence — each file is self-executing on import.
// Use the per-demo npm scripts (e.g. `npm run generics:identity`) to run just one.
import "./identity";
import "./filterArray";
import "./useFetch";
import "./extract";
import "./returnType";
import "./fetchJSON";
