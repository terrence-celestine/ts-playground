// Runs every utility-types demo in sequence — each file is self-executing on import.
// Use the per-demo npm scripts (e.g. `npm run utility-types:partial`) to run just one.
import "./renderPost";
import "./renderSummaries";
