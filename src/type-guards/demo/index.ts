// Runs every type-guards demo in sequence — each file is self-executing on import.
// Use the per-demo npm scripts (e.g. `npm run type-guards:string`) to run just one.
import "./isString";
import "./isBoolean";
import "./isAdmin";
import "./isCreateUserPayload";
